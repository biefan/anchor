# Eval sandbox directories — codex writes random project files here during runs;
# they're informative locally but shouldn't be tracked in the repo.
# (The eval output .txt and summary .json files in results/<ts>/ are still tracked.)
evals/results/*/sandbox-*/

# Python
__pycache__/
*.py[cod]

# Editors
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local-only experiments
local-*.md
*.bak
