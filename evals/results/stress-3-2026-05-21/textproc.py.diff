diff --git a/textproc.py b/textproc.py
index 116ac04..f5395f8 100644
--- a/textproc.py
+++ b/textproc.py
@@ -21,13 +21,15 @@ def truncate(s: str, max_len: int, suffix: str = "…") -> str:
     """Truncate s to max_len characters; if it gets shortened, append suffix."""
     if len(s) <= max_len:
         return s
-    return s[:max_len] + suffix
+    if max_len <= 0:
+        return ""
+    if len(suffix) >= max_len:
+        return suffix[:max_len]
+    return s[: max_len - len(suffix)] + suffix
 
 
 def word_count(s: str) -> int:
     """Count whitespace-separated words. Empty string -> 0."""
-    if not s:
-        return 1
     return len(s.split())
 
 
